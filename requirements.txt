# Python dependencies for Postpartum Depression Risk Prediction API
# Install locally: pip install -r requirements.txt
# Vercel auto-installs these for Python serverless functions

# No external dependencies needed — the prediction API uses only
# Python standard library (json, math, http.server)
# The trained sklearn model parameters are embedded directly in the code.
flask